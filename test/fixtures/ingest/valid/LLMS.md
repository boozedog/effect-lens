# Effect guidance

## Piping

Prefer `pipe` for composition.

```lens-guidance
applies-to: 4.0.0
```

## Layers

Prefer `Layer` for dependency management.

```lens-guidance
applies-to: 4.0.0..4.1.0
ref: v4.0.0-rc.110
```

## Testing

Use `@effect/vitest` with `it.effect` for Effect-first tests.
